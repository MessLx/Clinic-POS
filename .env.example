# Backend (API) — use when running backend outside Docker
# ConnectionStrings__Default=Host=localhost;Database=peaaura;Username=postgres;Password=postgres
# ConnectionStrings__Redis=localhost:6379
# ConnectionStrings__RabbitMQ=amqp://guest:guest@localhost:5672/
# Jwt__Key=your-secret-key-at-least-32-characters-long!!

# Frontend — use when running Next.js outside Docker
# NEXT_PUBLIC_API_URL=http://localhost:5000
